import { OptimizerOptions } from '../index'

import type { ModuleInfo } from 'rollup'

interface RollupPluginOptions extends OptimizerOptions {
  ignore?: (string | RegExp)[]
  shouldOptimize?: (module: ModuleInfo) => boolean
  isYfilesModule?: (module: ModuleInfo) => boolean
}

export default function YFilesOptimizerRollupPlugin(options?: RollupPluginOptions)
