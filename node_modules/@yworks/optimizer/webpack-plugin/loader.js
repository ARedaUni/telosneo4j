function loader(content) {
  const optimized = this.query && this.query.optimizedSource

  if (!optimized) {
    this.emitError(
      new Error(`yWorks Optimizer Loader: no optimized sources found for ${this.resourcePath}`)
    )
    return content
  }

  return optimized
}

module.exports = loader
