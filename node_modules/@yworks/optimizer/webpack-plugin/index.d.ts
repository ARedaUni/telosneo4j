import { OptimizerOptions } from '../index'

import type { NormalModule } from 'webpack'

interface WebpackPluginOptions extends OptimizerOptions {
  ignore?: (string | RegExp)[]
  shouldOptimize?: (module: NormalModule) => boolean
  isYfilesModule?: (module: NormalModule) => boolean
}

export default class YFilesOptimizerWebpackPlugin {
  constructor(options: WebpackPluginOptions)
}
